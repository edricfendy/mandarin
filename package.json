{
  "name": "mandarin-proficiency-trainer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-static.js",
    "check": "node --check app.js && node --check api/chat.js && node -e \"JSON.parse(require('fs').readFileSync('vercel.json', 'utf8')); console.log('vercel config ok')\""
  }
}
